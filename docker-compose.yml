services:

  mcp-mqc:
    build:
      context: .
      network: host
    command: uv run --env-file .env src/serveur_mcp.py
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - 8001:8001
    networks:
      - mqc-network

networks:
  mqc-network:
